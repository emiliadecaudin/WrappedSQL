WrappedSQL
==========

A simple library of classes to wrap around abstract SQL database connections, connections themselves, and cursors. Currently implemented against PyMySQL, with the intent that it will be genericized to work with multiple SQL libraries.